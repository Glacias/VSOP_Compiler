class A {
    v : Object <- new Object;
}

class B extends A {
    ut1 : unit <- ();
}

class Main extends B {
    main() : int32 {
        printBool(true or true);
        1 > 0;
        2 >= 2;
        1 > 0 or 2 >= 3;
        printBool(false xor true);
        printInt32(7 % 3);
        0
    }
}